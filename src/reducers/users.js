
const initialValue = {username: "", user_id: ""}
const users = (state=initialValue, action) => {
    switch(action.type) {
        case 'ON_LOGIN':
            return {}
    }
}

export default users 